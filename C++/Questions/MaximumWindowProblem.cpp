#include <iostream>
#include <vector>
#include <cmath>
using namespace std;
class solution
{
public
    string minWindow(string s, string t)
    {
        if (s == NULL || t == NULL || s.size() == 0 || s.size() == 0)
        {
            return "";
        }
    }
};
int main()
{
    int n, k;
    cin >> n >> k;
    vector<int> a(n);
    for (auto &i : a)
    {
        cin >> i;
    }

    return 0;
}